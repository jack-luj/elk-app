<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.github.jackl.elk.dao.CompanyDao">

	<select id="queryObject" resultType="com.github.jackl.elk.entity.Company">
		select * from t_company where id = #{value}
	</select>

	<select id="queryList" resultType="com.github.jackl.elk.entity.Company">
		select * from t_company 
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from t_company 
	</select>
	 
	<insert id="save" parameterType="com.github.jackl.elk.entity.Company" useGeneratedKeys="true" keyProperty="id">
		insert into t_company
		(
			`name`, 
			`website`
		)
		values
		(
			#{name}, 
			#{website}
		)
	</insert>
	 
	<update id="update" parameterType="com.github.jackl.elk.entity.Company">
		update t_company 
		<set>
			<if test="name != null">`name` = #{name}, </if>
			<if test="website != null">`website` = #{website}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from t_company where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from t_company where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>